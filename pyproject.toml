[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "latexfigure"
version = "1.0.0"
dependencies = [
    "latexfigure",
]

[tool.setuptools]
py-modules = ["latexfigure"]
